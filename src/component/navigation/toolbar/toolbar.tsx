import { Logo } from "../../logo/logo";
import "./toolbar.css";

export const ToolbarComponent = () => (
  <header className="Toolbar">
    <div>Menu</div>
    <Logo />
    <nav>links</nav>
  </header>
);
